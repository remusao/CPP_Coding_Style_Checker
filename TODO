
- Fix locations counting (lines ?)
- Supports every errors

- Check class name with file name
- Check that there is only one class def per file
- header protection against multiple inclusion
- using name space (check if we are in a header)
- check indentation (closing brace on the same column than opening one)
- binary operator padding
- Check big comment (no dead code)
- operator() and [] must not be padded
- Spanned line must be cut befor binary op.
- Nb lines by function
